{
  "name": "testserver",
  "version": "1.0.0",
  "description": "Testdata for sockettest",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "KZE",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.12.1",
    "socket.io": "^2.0.4"
  }
}
